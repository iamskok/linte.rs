import Layout from "../components/Layout";
import commitlintOutput from "../images/commitlint-output.gif";

<img src={commitlintOutput} alt="Commitlint output" />

export default ({ children }) => (
  <Layout>{children}</Layout>
);

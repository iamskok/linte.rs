import Layout from "../components/Layout";
import eslintOutputSrc from "../images/eslint-output.gif";

<img src={eslintOutputSrc} alt="ESLint output" />

export default ({ children }) => (
  <Layout>{children}</Layout>
);

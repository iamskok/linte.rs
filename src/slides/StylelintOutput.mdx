import Layout from "../components/Layout";
import stylelintOutputSrc from "../images/stylelint-output.gif";

<img src={stylelintOutputSrc} alt="Stylelint output" />

export default ({ children }) => (
  <Layout>{children}</Layout>
);

import Layout from "../components/Layout";
import eslintInitSrc from "../images/eslint-init.gif";

<img src={eslintInitSrc} alt="ESLint init CLI" />

export default ({ children }) => (
  <Layout>{children}</Layout>
);

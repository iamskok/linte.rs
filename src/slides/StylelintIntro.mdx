import Layout from "../components/Layout";
import stylelintLogoSrc from "../images/stylelint-logo.svg";

<div
  style={{
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-around",
  }}
>
  <h1>Stylelint</h1>
  <div
    style={{
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
    }}
  >
    <img style={{ width: 200 }} src={stylelintLogoSrc} alt="Stylelint logo" />
  </div>
</div>

export default ({ children }) => <Layout>{children}</Layout>;

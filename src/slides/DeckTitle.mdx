import Layout from "../components/Layout";

# Linters for all the things

## Tools to improve code style and quality by enforcing conventions

export default ({ children }) => (
  <Layout>{children}</Layout>
);
